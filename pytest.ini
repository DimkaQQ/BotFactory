[pytest]
testpaths = tests
asyncio_mode = auto
# Every async fixture here is per-test; without this each one would warn.
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:aiogram.*
